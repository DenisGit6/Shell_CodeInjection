An example of simple hacking into a shell script of injecting precise true statements to bypass the true condition
------------------------------------------------------------
# To inject and hack this if statement - type in bash terminal "1=1 -o x "
------------------------------------------------------------
